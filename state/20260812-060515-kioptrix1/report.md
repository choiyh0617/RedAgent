# Pentest Report: 20260812-060515-kioptrix1

PENETRATION TEST REPORT
침투 테스트 보고서
Kioptrix Level 1 — VirtualBox 격리 랩 환경 실습

| 항목 | 내용 |
|---|---|
| 테스트 대상 | Kioptrix Level 1 (VirtualBox VM, 호스트명: KIOPTRIX) |
| 테스트 목적 | 학습용 로컬 랩 환경에서의 침투 테스트 실습 (Boot2root) |
| 테스트 환경 | VirtualBox Host-only Network (192.168.56.0/24), 인터넷 비공개 격리망 |
| 공격 시스템 | Kali Linux 2026.2 (192.168.56.101) |
| 대상 시스템 | Kioptrix Level 1 (VirtualBox VM, 호스트명: KIOPTRIX) |
| 테스트 기간 | 2026-08-12 ~ 2026-08-12 |
| 승인 범위 | 본인 소유 로컬 VirtualBox 랩 내 VM 1대, 외부 네트워크 미포함 |
| 작성일 | 2026-08-12 |

## 1. 개요 (Executive Summary)
- 이번 보고서는 자동 점검 과정에서 발견된 노출 정보와 시도 결과를 정리한 것이다.
- 직접 침투 성공이 없더라도, 잠재적 취약 지점은 운영 환경 기준으로 후속 확인이 필요하다.

이번 보고서는 자동화된 파이프라인이 수집한 증적(findings.jsonl)을 기반으로, 비보안 담당자도 이해할 수 있게 재구성한 결과다.

## 2. 테스트 범위 및 승인
- 대상: Kioptrix Level 1 (VirtualBox VM, 호스트명: KIOPTRIX)
- 네트워크: VirtualBox Host-only Network (192.168.56.0/24), 인터넷 비공개 격리망
- 승인: 본인 소유 로컬 VirtualBox 랩 내 VM 1대, 외부 네트워크 미포함
- 제외 범위: 서비스 거부(DoS), 물리적 접근, 승인되지 않은 외부 네트워크 테스트

## 3. 방법론
- 1단계 — 정보수집(Reconnaissance): Nmap 기반 포트/서비스 식별 및 배너 수집
- 2단계 — 서비스 열거(Enumeration): HTTP/SMB/FTP 등 노출 서비스별 추가 정보 수집
- 3단계 — 취약점 분석: 배너와 공개 PoC/Exploit-DB 기준으로 악용 가능성 평가
- 4단계 — 익스플로잇 검증: 자동화된 PoC 또는 프레임워크를 사용해 실제 재현 여부 확인

## 4. 정찰 및 서비스 스캔 결과
공격자 시스템 `Kali Linux 2026.2 (192.168.56.101)` 에서 대상 `192.168.56.103` 에 대해 포트 및 서비스 식별을 수행한 결과는 다음과 같다:

| 포트/프로토콜 | 서비스 | 버전 정보 |
|---|---|---|
| 22/tcp | ssh | OpenSSH 2.9p2 (protocol 1.99) |
| 80/tcp | http | Apache httpd 1.3.20 ((Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b) |
| 111/tcp | rpcbind | 2 (RPC #100000) |
| 139/tcp | netbios-ssn | Samba smbd (workgroup: eMYGROUP) |
| 443/tcp | ssl/https | Apache/1.3.20 (Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b |
| 32768/tcp | status | 1 (RPC #100024) |

추가 열거 결과:
- HTTP 열거(80/tcp): http://192.168.56.103:80 [200 OK] Apache[1.3.20][mod_ssl/2.8.4], Country[RESERVED][ZZ], Email[webmaster@example.com], HTTPServer[Red Hat Linux][Apache/1.3.20 (Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b], IP[192....
- 웹 경로 탐색(80/tcp): .htaccess            (Status: 403) [Size: 273] .htpasswd            (Status: 403) [Size: 273] .hta                 (Status: 403) [Size: 268] ~operator            (Status: 403) [Size: 273] ~root                (Status: 40...
- SMB 열거(139/tcp): {'enum4linux': ''}

## 5. 발견된 취약점

| 번호 | 취약점 | 심각도 | 상태 |
|---|---|---|---|
| F-1 | Samba 노출 및 원격 코드 실행 가능성 | High | 확인됨 |
설명: Samba smbd 서비스가 외부에 노출되어 있으며, 오래된 Kioptrix 계열 환경에서 대표적인 핵심 공격 표면이다.
| F-2 | Apache/mod_ssl/OpenSSL 구버전 사용 | Critical | 확인됨 |
설명: Apache 1.3.20 + mod_ssl 2.8.4 + OpenSSL 0.9.6b 조합은 이미 지원 종료되었고 알려진 공개 취약점과 연관된다.
| F-3 | 전반적인 서비스 스택 노후화 | High | 확인됨 |
설명: 여러 핵심 서비스가 모두 오래된 버전으로 노출되어 단일 취약점이 아니라 구조적 위험으로 해석된다.

## 6. 익스플로잇 상세 내역
### 6.1 자동 악용 시도 결과
이번 실행에서는 자동화된 경로로 즉시 셸 획득까지 이어진 성공 사례는 확인되지 않았다.

### 6.2 추가 시도 및 제약
- `SQL Injection (sqlmap --forms)` / port `80`: sqlmap이 인젝션 포인트를 찾지 못함
- `SQL Injection (sqlmap --forms)` / port `443`: sqlmap이 인젝션 포인트를 찾지 못함

## 7. 권고사항

| 대상 | 권고 조치 | 우선순위 |
|---|---|---|
| Samba 노출 | Samba 최신 버전 업그레이드 또는 서비스 제거, 불가피하면 신뢰된 호스트로 접근 제한 | 긴급 |
| mod_ssl / OpenSSL | Apache, mod_ssl, OpenSSL 최신화 및 SSLv2/SSLv3 비활성화, TLS 1.2+만 허용 | 긴급 |
| EOL 소프트웨어 | 전체 OS/패키지 마이그레이션 및 정기 패치 프로세스 수립 | 높음 |

## 8. 결론
이번 실행 결과는 단순 포트 노출 수준이 아니라, 오래된 서비스 스택과 취약 가능성이 함께 존재하는 구조적 문제를 보여준다.
실제 운영 환경에서 같은 결과가 발견된다면, 취약 서비스의 즉시 격리, 최신 버전 업그레이드, 접근통제 강화가 우선이다.

## 9. 단계별 상세 결과

## Summary

- 대상: 192.168.56.103
- 진행된 단계: exploitation, scanning, vuln_analysis
- 총 findings: 39

### 비전문가용 한줄 요약

- 이번 보고서는 자동 점검 과정에서 발견된 노출 정보와 시도 결과를 정리한 것이다.
- 직접 침투 성공이 없더라도, 잠재적 취약 지점은 운영 환경 기준으로 후속 확인이 필요하다.

### 이 보고서를 어떻게 읽으면 되는가

- `무엇이 실제로 가능했는가`: 공격자가 어디까지 접근했는지 보여준다.
- `왜 문제인가`: 비보안 담당자 기준으로 사업/운영 관점의 위험을 설명한다.
- `권장 조치`: 개발팀, 운영팀, 보안팀이 바로 실행할 수 있는 후속 대응을 적었다.


## Scanning

### 192.168.56.103
- 플랫폼 추정: `linux`
- 해석: 리눅스 계열로 추정됐다.
  - 원인: 열린 포트와 서비스 조합이 리눅스 서버에서 흔히 보이는 형태와 유사했다.
  - 보안 권고: 불필요한 서비스 종료, 패키지 패치, 외부 공개 포트 최소화가 기본 대응이다.

| Port | Service | Banner |
|---|---|---|
| 22 | ssh | OpenSSH 2.9p2 (protocol 1.99) |
| 80 | http | Apache httpd 1.3.20 ((Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b) |
| 111 | rpcbind | 2 (RPC #100000) |
| 139 | netbios-ssn | Samba smbd (workgroup: eMYGROUP) |
| 443 | ssl/https | Apache/1.3.20 (Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b |
| 32768 | status | 1 (RPC #100024) |

#### 포트별 해석

- `port 22` / `ssh`
  - 원인: 원격 관리 서비스는 외부에서 직접 로그인 시도가 가능해 계정 탈취와 취약 버전 악용 위험이 있다.
  - 보안 권고: 허용 IP 제한, MFA 또는 키 기반 인증, 구형 프로토콜 비활성화가 필요하다.
- `port 80` / `http`
  - 원인: 웹 서비스가 외부에 노출되어 있어 입력값 검증, 인증, 권한검사 문제가 공격 표면이 된다.
  - 보안 권고: WAF에만 의존하지 말고 서버 코드에서 입력 검증, 인증, 인가를 구현하고 관리자 기능을 별도 보호해야 한다.
- `port 111` / `rpcbind`
  - 원인: 외부에 열린 서비스는 그 자체로 공격 표면이 되며, 버전 노후화나 기본 설정 미흡이 문제로 이어질 수 있다.
  - 보안 권고: 서비스 필요성 검토, 최신 패치 적용, 접근 가능한 네트워크 대역 최소화가 기본 대응이다.
- `port 139` / `netbios-ssn`
  - 원인: 파일 공유와 인증 관련 서비스는 자격증명 탈취, 원격 실행, 내부 이동의 핵심 경로가 된다.
  - 보안 권고: 외부 노출 금지, SMB 서명 정책 점검, 관리자 공유 최소화가 필요하다.
- `port 443` / `ssl/https`
  - 원인: 웹 서비스가 외부에 노출되어 있어 입력값 검증, 인증, 권한검사 문제가 공격 표면이 된다.
  - 보안 권고: WAF에만 의존하지 말고 서버 코드에서 입력 검증, 인증, 인가를 구현하고 관리자 기능을 별도 보호해야 한다.
- `port 32768` / `status`
  - 원인: 외부에 열린 서비스는 그 자체로 공격 표면이 되며, 버전 노후화나 기본 설정 미흡이 문제로 이어질 수 있다.
  - 보안 권고: 서비스 필요성 검토, 최신 패치 적용, 접근 가능한 네트워크 대역 최소화가 기본 대응이다.
- `http_enum` 결과: {'whatweb': 'http://192.168.56.103:80 [200 OK] Apache[1.3.20][mod_ssl/2.8.4], Country[RESERVED][ZZ], Email[webmaster@example.com], HTTPServer[Red Hat Linux][Apache/1.3.20 (Unix)  (Red-Hat/Linux) mod_ssl/2.8.4 OpenSSL/0.9.6b], IP[192.168.56.103], OpenSSL[0.9.6b], Title[Test Page for the Apache Web Se...
- `smb_enum` 결과: {'enum4linux': ''}
- `http_enum` 결과: {'whatweb': '', 'gobuster': ''}


## Vulnerability Analysis

### 192.168.56.103

- **port 139 (netbios-ssn)** - confidence 0.00, risk `low` - searchsploit 검색 결과가 빈 배열이므로 이 Samba smbd 버전/배너에 매칭되는 공개 PoC나 익스플로잇 코드가 전혀 존재하지 않는다. 평가할 실제 코드가 없으므로 코드 수정 없이 자동 실행 가능한 기존 익스플로잇은 없다고 판단하며, 자동화 파이프라인 상 이 경로로는 즉시 이용 가능한 위협이 확인되지 않는다.
  - 원인: searchsploit 검색 결과가 빈 배열이므로 이 Samba smbd 버전/배너에 매칭되는 공개 PoC나 익스플로잇 코드가 전혀 존재하지 않는다. 평가할 실제 코드가 없으므로 코드 수정 없이 자동 실행 가능한 기존 익스플로잇은 없다고 판단하며, 자동화 파이프라인 상 이 경로로는 즉시 이용 가능한 위협이 확인되지 않는다.
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 파일 공유와 인증 관련 서비스는 자격증명 탈취, 원격 실행, 내부 이동의 핵심 경로가 된다.
  - 보안 권고: 외부 노출 금지, SMB 서명 정책 점검, 관리자 공유 최소화가 필요하다.
- **port 22 (ssh)** - confidence 0.00, risk `low` - searchsploit 검색 결과가 빈 배열이라 이 서비스/배너에 대응하는 공개 PoC/모듈 자체가 없다. 따라서 코드 수정 여부를 논할 대상 코드가 없으며, 자동 실행 가능한 기존 익스플로잇은 존재하지 않는다. 배너 문자열(OpenSSH 2.9p2 protocol 1.99)이 실제 취약 버전과 일치하는지 별도 수동 검증이 필요하다.
  - 원인: searchsploit 검색 결과가 빈 배열이라 이 서비스/배너에 대응하는 공개 PoC/모듈 자체가 없다. 따라서 코드 수정 여부를 논할 대상 코드가 없으며, 자동 실행 가능한 기존 익스플로잇은 존재하지 않는다. 배너 문자열(OpenSSH 2.9p2 protocol 1.99)이 실제 취약 버전과 일치하는지 별도 수동 검증이 필요하다.
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 원격 관리 서비스는 외부에서 직접 로그인 시도가 가능해 계정 탈취와 취약 버전 악용 위험이 있다.
  - 보안 권고: 허용 IP 제한, MFA 또는 키 기반 인증, 구형 프로토콜 비활성화가 필요하다.
- **port 80 (http)** - confidence 0.00, risk `low` - 제공된 searchsploit 결과가 빈 배열([])로, 이 서비스/버전에 대해 참조할 수 있는 공개 PoC 코드 자체가 없다. 평가할 익스플로잇이 없으므로 코드 수정 없이 자동 실행이 가능한지 판단할 근거가 전혀 없으며, 따라서 confidence는 최저치로 설정한다. (참고로 Apache 1.3.20/mod_ssl 2.8.4/OpenSSL 0.9.6b 조합은 실제로는 취약한 것으로 알려져 있으나, 그 정보가 이번 검색 결과에 포함되어 있지 않으므로 이 평가에는 반영하지 않았다.)
  - 원인: 제공된 searchsploit 결과가 빈 배열([])로, 이 서비스/버전에 대해 참조할 수 있는 공개 PoC 코드 자체가 없다. 평가할 익스플로잇이 없으므로 코드 수정 없이 자동 실행이 가능한지 판단할 근거가 전혀 없으며, 따라서 confidence는 최저치로 설정한다. (참고로 Apache 1.3.20/mod_ssl 2.8.4/OpenSSL 0.9.6b 조합은 실제로는 취약한 것으로 알려져 있으나, 그 정보가 이번 검색 결과에 포함되어 있지 않으므로 이 평가에는 반영하지 않았다.)
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 웹 서비스가 외부에 노출되어 있어 입력값 검증, 인증, 권한검사 문제가 공격 표면이 된다.
  - 보안 권고: WAF에만 의존하지 말고 서버 코드에서 입력 검증, 인증, 인가를 구현하고 관리자 기능을 별도 보호해야 한다.
- **port 111 (rpcbind)** - confidence 0.00, risk `low` - searchsploit 검색 결과가 빈 배열이므로 rpcbind(포트 111, RPC #100000)에 대해 매칭되는 공개 Exploit-DB 항목 자체가 없다. 평가할 PoC 코드가 존재하지 않으므로 코드 수정 없이 바로 통할 익스플로잇이 있다고 판단할 근거가 전혀 없어 confidence는 0에 가깝다. rpcbind는 그 자체로 원격 코드실행 취약점보다는 정보노출/리플렉션(DDoS 증폭) 용도로 주로 다뤄지므로, 별도 취약점이 확인되지 않는 한 위험도는 낮게 분류한다.
  - 원인: searchsploit 검색 결과가 빈 배열이므로 rpcbind(포트 111, RPC #100000)에 대해 매칭되는 공개 Exploit-DB 항목 자체가 없다. 평가할 PoC 코드가 존재하지 않으므로 코드 수정 없이 바로 통할 익스플로잇이 있다고 판단할 근거가 전혀 없어 confidence는 0에 가깝다. rpcbind는 그 자체로 원격 코드실행 취약점보다는 정보노출/리플렉션(DDoS 증폭) 용도로 주로 다뤄지므로, 별도 취약점이 확인되지 않는 한 위험도는 낮게 분류한다.
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 외부에 열린 서비스는 그 자체로 공격 표면이 되며, 버전 노후화나 기본 설정 미흡이 문제로 이어질 수 있다.
  - 보안 권고: 서비스 필요성 검토, 최신 패치 적용, 접근 가능한 네트워크 대역 최소화가 기본 대응이다.
- **port 443 (ssl/https)** - confidence 0.00, risk `low` - 제공된 searchsploit 결과가 빈 배열이라 매칭되는 공개 PoC/익스플로잇 코드가 전혀 없어 자동 실행 가능한 항목 자체가 존재하지 않는다. Apache 1.3.20/mod_ssl 2.8.4/OpenSSL 0.9.6b 조합은 역사적으로 mod_ssl 원격취약점(예: Slapper 웜, CVE-2002-0082) 등이 알려져 있으나 이번 검색 결과에는 해당 코드가 포함되어 있지 않으므로 이 데이터만으로는 코드수정 없는 즉시 실행을 평가할 근거가 없다. 실제 시도를 원한다면 별도로 해당 CVE의 PoC를 검색/확보한 뒤 오프셋·타겟 바이너리·OpenSSL 버전 문자열 등을 이 정확한 빌드에 맞게 재검증해야 한다.
  - 원인: 제공된 searchsploit 결과가 빈 배열이라 매칭되는 공개 PoC/익스플로잇 코드가 전혀 없어 자동 실행 가능한 항목 자체가 존재하지 않는다. Apache 1.3.20/mod_ssl 2.8.4/OpenSSL 0.9.6b 조합은 역사적으로 mod_ssl 원격취약점(예: Slapper 웜, CVE-2002-0082) 등이 알려져 있으나 이번 검색 결과에는 해당 코드가 포함되어 있지 않으므로 이 데이터만으로는 코드수정 없는 즉시 실행을 평가할 근거가 없다. 실제 시도를 원한다면 별도로 해당 CVE의 PoC를 검색/확보한 뒤 오프셋·타겟 바이너리·OpenSSL 버전 문자열 등을 이 정확한 빌드에 맞게 재검증해야 한다.
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 웹 서비스가 외부에 노출되어 있어 입력값 검증, 인증, 권한검사 문제가 공격 표면이 된다.
  - 보안 권고: WAF에만 의존하지 말고 서버 코드에서 입력 검증, 인증, 인가를 구현하고 관리자 기능을 별도 보호해야 한다.
- **port 32768 (status)** - confidence 0.00, risk `low` - searchsploit 검색 결과가 빈 배열([])로 해당 서비스/버전에 매칭되는 공개 PoC가 Exploit-DB에 존재하지 않는다. 평가할 실제 코드가 없으므로 코드 수정 없이 그대로 통할 기성 익스플로잇은 없다고 판단하며, 자동 실행 가능한 항목이 전무해 confidence는 0에 가깝다.
  - 원인: searchsploit 검색 결과가 빈 배열([])로 해당 서비스/버전에 매칭되는 공개 PoC가 Exploit-DB에 존재하지 않는다. 평가할 실제 코드가 없으므로 코드 수정 없이 그대로 통할 기성 익스플로잇은 없다고 판단하며, 자동 실행 가능한 항목이 전무해 confidence는 0에 가깝다.
  - 판단 근거: 이번 단계는 실제 공격 성공 보고가 아니라, '공격 후보로 볼 만한 이유'를 정리한 단계다.
  - 원인: 외부에 열린 서비스는 그 자체로 공격 표면이 되며, 버전 노후화나 기본 설정 미흡이 문제로 이어질 수 있다.
  - 보안 권고: 서비스 필요성 검토, 최신 패치 적용, 접근 가능한 네트워크 대역 최소화가 기본 대응이다.


## Exploitation

### 192.168.56.103

- **[실패]** port 80 - SQL Injection (sqlmap --forms) (방법: SQLi(sqlmap))
  - 원인: 사용자 입력이 SQL 질의문에 안전하게 바인딩되지 않아, 입력값이 명령처럼 해석됐다.
  - 영향: 공격자가 비밀번호를 몰라도 로그인 우회, 데이터 조회, 경우에 따라 데이터 변경까지 시도할 수 있다.
  - 보안 권고: Prepared Statement 적용, ORM 파라미터 바인딩 강제, 관리자 기능 쿼리 전수 점검이 필요하다.
- **[실패]** port 443 - SQL Injection (sqlmap --forms) (방법: SQLi(sqlmap))
  - 원인: 사용자 입력이 SQL 질의문에 안전하게 바인딩되지 않아, 입력값이 명령처럼 해석됐다.
  - 영향: 공격자가 비밀번호를 몰라도 로그인 우회, 데이터 조회, 경우에 따라 데이터 변경까지 시도할 수 있다.
  - 보안 권고: Prepared Statement 적용, ORM 파라미터 바인딩 강제, 관리자 기능 쿼리 전수 점검이 필요하다.
